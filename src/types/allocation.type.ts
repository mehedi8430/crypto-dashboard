export type Allocation = {
  name: string;
  value: number;
  fill: string;
};

export type TAllocationKeyData = {
  starting_balance: number;
  ending_balance: number;
  notes: string;
  createdAt: string;
};
